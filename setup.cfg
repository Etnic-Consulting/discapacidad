# Coverage path mapping para tests SMT-ONIC
# Permite --cov=backend/app/routers/pueblos cuando los módulos se importan como app.routers.*
[coverage:paths]
source =
    app
    backend/app
    */backend/app
